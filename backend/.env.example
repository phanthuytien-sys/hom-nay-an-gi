# Cổng chạy server (mặc định 3000)
PORT=3000

# Google Maps / Places API key — để dùng API /api/places lấy quán ăn thật.
# Lấy key tại: https://console.cloud.google.com/  → bật "Places API"
# Nếu để trống, /api/places sẽ tự dùng dữ liệu quán mẫu trong máy (fallback).
GOOGLE_MAPS_API_KEY=
